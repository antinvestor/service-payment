linters-settings:
  errcheck:
    exclude-functions:
      - fmt.Errorf  # ✅ `errcheck` supports settings

run:
  timeout: 5m

linters:
  disable:
    - gocritic
    - godox
  enable:
    - bidichk
    - durationcheck
    - errcheck
    - errorlint
    - goimports
    - goconst
    - godot
    - goheader
    - gomoddirectives
    - gomodguard
    - goprintffuncname
    - gosec
    - ineffassign
    - gosimple
    - govet
    - nolintlint
    - predeclared
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - testpackage
    - typecheck
    - unconvert
    - unparam
    - unused
    - wastedassign
    - whitespace
    - usetesting
    - copyloopvar

issues:
  fix: true
